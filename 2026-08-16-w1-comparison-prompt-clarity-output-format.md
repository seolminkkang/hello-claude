---
date: 2026-08-16
week: 1
session: S2
practice_type:
  - comparison
topic:
  - prompt-clarity
  - output-format
model: claude-sonnet
status: verified
result: explicit-json-schema-plus-example-preserved-accuracy-while-forcing-parseable-output
---

# 실험 목적
"모호한 요청→명세 변환, 예시·제약·출력형식의 영향" 개념 학습 후, 같은 교정 작업을
모호하게 시켰을 때와 출력 형식(JSON 스키마)+예시를 명시했을 때 결과가 실제로
어떻게 달라지는지, 특히 형식 강제가 정확성을 희생시키는지 확인.

## 가설
같은 교정 작업이라도 출력 형식(JSON 스키마)과 예시를 명시하면 (1) 실제로 그 형식을
지키고 (2) 형식 강제가 답변의 정확성을 희생시키지는 않을 것이다.

## 조건
- **버전 A (모호)**: `"이 문장 좀 교정해줘: I have went to school yesterday"`
- **버전 B (명확 + 제약 + 예시)**: `<instructions>`로 출력 스키마(`{"correction","explanation"}`)
  지정 + `<example>` 1개 제공 + 동일 문장 입력

## 고정한 변수
교정 대상 문장(`I have went to school yesterday`), 모델(Claude Sonnet 5),
실행 방식(각각 새 대화·컨텍스트 없음)

## 변경한 변수
프롬프트의 명확성 — 출력 형식 지정 여부, 예시(example) 유무

## 평가 기준
1. 문법 교정 자체의 정확성
2. 출력이 요청한 형식(JSON)을 따르고 코드로 파싱 가능한지
3. 설명의 깊이/구조

## 실행 결과
| | 버전 A (모호) | 버전 B (JSON 스키마+예시) |
|---|---|---|
| 정확성 | ✅ (`have went`→`went`, 이유까지 정확) | ✅ (동일하게 정확) |
| 형식 | 자유 마크다운(bullet, 볼드, 비교 예문 추가) | 요청한 JSON 그대로 |
| 파싱 가능 여부 | 불가능 (자유 텍스트) | 가능 |
| 설명 깊이 | 두 문법 포인트를 나눠 상세 설명 + 추가 예문 자발적으로 첨부 | 한 문장으로 압축 |

## 예상과 달랐던 점
"출력 형식을 강제하면 설명이 부실해지거나 정확도가 떨어지지 않을까"라는 우려가
있었는데, 이번 1건에서는 **정확도 손실 없이 형식만 원하는 대로 바뀜**. 다만 설명은
더 압축됨(깊이가 아니라 서술 분량이 줄어든 것에 가까움).

## 실패 사례
이번 조건에서는 형식 위반이나 오답 사례를 발견하지 못함. 실험 설계 단계에서 실수를
하나 발견함: 처음에는 A를 "기능을 개선해줘"(설계 요청)로, B를 "이 문장을 교정해줘"
(실행 요청)로 서로 다른 작업으로 설정했다가, 작업 자체가 다르면 비교가 무의미하다는
점을 뒤늦게 알아채고 A도 동일한 문장 교정 요청으로 수정함 — 실험 변수를 통제할 때
"모델"뿐 아니라 "작업(task) 자체"도 고정해야 한다는 걸 실습으로 확인.

## 결론
프롬프트에 출력 형식(JSON 스키마)과 예시를 명시하는 것은, 최소한 이번 케이스에서는
정확도 손해 없이 파싱 가능한 결과를 얻는 데 유효했다. 단, **표본이 문장 1개뿐이라
일반화하기엔 이르고**, 이 결론은 잠정적인 첫 데이터 포인트로 취급해야 함.

## 실제 프로젝트 적용 가능성
`ai/StructuredResponse` / `CoachingService` 설계 시, Claude의 자유 텍스트 응답을
정규식 등으로 파싱하려 시도하기보다, 프롬프트에서 JSON 스키마와 예시를 직접
요구하는 방식을 우선 채택할 근거가 됨. 단, 실제 채택 전 S4에서 더 많은 입력
케이스로 재검증 필요.

## 재현 방법
새 대화(컨텍스트 없음) 두 개를 열고, 동일 모델(Claude Sonnet 5)로 위 버전 A/B
프롬프트를 각각 입력해 출력 형식과 정확성을 비교.
