---
date: 2026-08-17
week: 1
session: S3
practice_type:
  - comparison
topic:
  - context-engineering
  - claude-md
model: claude-sonnet
status: draft
result: project-specific-context-loads-without-repeating-in-prompt-but-source-not-isolated
---

# 실험 목적
"필요한 정보를 Prompt/CLAUDE.md/Memory/RAG 중 어디에 배치할지" 판단 원리를 배운 뒤,
CLAUDE.md 같은 프로젝트 지침이 실제로 매 요청마다 반복 설명하지 않아도 자동으로
로드되는지 직접 확인.

## 가설
프로젝트 루트의 CLAUDE.md에 적힌 내용은, 프롬프트에 별도로 첨부하지 않아도 해당
프로젝트 세션에서는 자동으로 참조된다.

## 조건
다른(별도) 프로젝트의 Claude Code 세션에서, 프로젝트 설명을 전혀 첨부하지 않고
다음 질문만 던짐: `"이 프로젝트의 핵심 원칙이 뭐야?"`

## 고정한 변수
질문 문구, 첨부 자료 없음(순수 질문만)

## 변경한 변수
없음 (단일 조건 확인)

## 평가 기준
답변이 그 프로젝트 고유의 구체적 내용을 담고 있는지 (일반론이 아니라 해당
프로젝트에만 있는 표현/규칙을 인용하는지)

## 실행 결과
같은 질문을 대상 프로젝트(이력서/포트폴리오 수정 작업, "델타클럽")의 Claude Code
세션에서 던졌을 때 나온 답변을 정리하면:

1. **목적** — 진짜 이력서·포트폴리오 수정이 최종 목적이며, 스터디 활동은 수단일 뿐.
   품질 기준은 "시니어가 읽고 문제를 뾰족하게 정의해서 잘 풀었다고 느끼는가"이고,
   판단이 갈리면 항상 기업 제출용 기준을 따름.
2. **판단을 A/B로 분리** — A(작업 후보 선정: 성능·규모·정합성·자동화, 측정 가능성
   기준)와 B(이력서 게재 선정: 실재+숫자 목표+Before/After+트레이드오프)를 섞지 않고,
   판단 시작 시 어느 쪽인지 먼저 선언.
3. **작업 방향은 역방향** — 기존 코드에서 쓸 거리를 찾는 게 아니라, 증명할 문장을
   먼저 정하고 그걸 증명하는 데 필요한 것을 만듦.
4. **정직성 선** — 목표는 지어내는 게 아니라 선언하는 것("1000명이 썼다"가 아니라
   "1000명 동시를 목표로 잡고 검증했다"고 표현).
5. **사실은 묻고 판단만 함** — 서비스 동작은 코드 확인이나 질문으로 채우고, 모르면
   추측하지 않고 멈춤.
6. **결론을 반론만으로 뒤집지 않음** — 새 사실이 나왔을 때만 바꾸고, 무엇이
   바뀌었는지 명시.
7. **막혔다고 프로젝트를 갈아타지 않음** — 문제 정의 조정은 정상이나 프로젝트
   교체는 회피로 간주.
8. **속도 우선** — 진단은 충분히 됐으면 세 번째 진단으로 들어가지 않고 바로 수정.

이 내용은 그 프로젝트의 CLAUDE.md 없이는 나올 수 없는 수준으로 구체적이었음
(프로젝트 고유 용어·판단 프레임워크 그대로 인용됨).

## 예상과 달랐던 점
답변 말미에 "이 둘(A/B 혼동, 반론에 결론 뒤집기)은 메모리에도 따로 남겨져 있다"는
언급이 포함되어 있었음 — 즉 이번 확인에서 로드된 내용이 CLAUDE.md만이 아니라
자동 메모리(auto memory)와 함께 섞여 있었을 가능성이 있음.

## 실패 사례
해당 없음 (형식보다는 소스 분리 여부가 남은 과제).

## 결론
"프로젝트 지침이 프롬프트 반복 설명 없이 자동으로 컨텍스트에 실린다"는 가설은
지지되나, 이번 확인만으로는 그 출처가 CLAUDE.md 단독인지 auto memory와의 결합
효과인지까지는 분리하지 못함. 또한 "로드 안 된 상태"와 나란히 비교하는 baseline
실행을 하지 않아 대조군이 없음.

## 실제 프로젝트 적용 가능성
`english-coach-ai-lab`에 프로젝트 전역 규칙(예: 응답 톤, 저장 시점 기준)을 둘 때,
매 프롬프트에 반복하지 않고 CLAUDE.md에 한 번만 적어두는 설계가 유효함을
뒷받침하는 근거로 사용 가능.

## 재현 방법
1. CLAUDE.md도 auto memory 항목도 전혀 없는 새 프로젝트 폴더에서 동일 질문을 던져
   "모른다"는 답이 나오는지 baseline으로 확인.
2. CLAUDE.md만 있고 auto memory는 없는 상태에서 같은 질문을 던져 결과 비교.
3. 위 결과와 이번 실행 결과를 나란히 놓고 CLAUDE.md 단독 효과를 분리.
